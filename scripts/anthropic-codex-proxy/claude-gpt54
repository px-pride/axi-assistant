#!/usr/bin/env bash
set -euo pipefail

export ANTHROPIC_API_KEY="${ANTHROPIC_API_KEY:-test}"
export ANTHROPIC_BASE_URL="${ANTHROPIC_BASE_URL:-http://127.0.0.1:3000}"
export ANTHROPIC_MODEL="${ANTHROPIC_MODEL:-gpt-5.4}"

exec claude "$@"
