# Naming

Method and function names must never be so similar as to create ambiguity. If two functions do different things, their names must make the difference obvious at a glance. Avoid pairs like `sleep()` vs `sleep_agent()`, `send()` vs `send_message()`, or `close()` vs `close_connection()` where the relationship and distinction aren't immediately clear.
